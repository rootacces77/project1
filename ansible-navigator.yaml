ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg 

  execution-environment:
    container-engine: docker
    enabled: true
    image: project-env:1.0
    pull:
      policy: missing

#  logging:


  playbook-artifact:
    enable: false
