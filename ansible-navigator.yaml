ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg 

  execution-environment:
    container-engine: docker
    enabled: true
    image: project1-env:latest
    pull:
      policy: missing

#  logging:


  playbook-artifact:
    enable: false
